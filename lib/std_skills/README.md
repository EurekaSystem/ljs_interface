# std_skills

Standard message definition for PAL skills (PAPS-013).

This package includes:
- standard metadata type (`Meta`, `Result`, `Feedback`) used by topic, service and action-based skills
- skill definitions for common datatype, mimicking ROS' `std_msgs` and `std_srvs`
